[package]
name = "vmsa-test-harness"
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[lib]
path = "src/lib.rs"
test = false
bench = false

[dependencies]
aarch64-vmsa = { path = "../external/aarch64-vmsa" }
vmsa-test-architecture = { path = "../architecture" }
